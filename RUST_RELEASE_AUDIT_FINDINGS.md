# Rust Release Audit Findings

## Decision Snapshot

- Release claim allowed now: `YES (conditional)`
- Allowed external claim: `Rust minimal compatibility client release chain is working`
- Disallowed external claim: `full Java desktop client parity is complete`
- Current decision status: `resolved` (frozen go/no-go checklist is defined below)

## Status Legend

- `resolved`: landed and locally validated with evidence
- `in-progress`: partially closed, remaining closure work exists
- `unresolved`: not closed yet (not started or waiting decision/owner)

## Severity Matrix

| ID | Severity | Status | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F-001 | critical | resolved | Release messaging scope was ambiguous | `tools/WINDOWS-RELEASE.md:3`; `tools/README.md:13-14`; `README.md:31` |
| F-002 | critical | resolved | Release docs had machine-specific absolute command assumptions | `tools/WINDOWS-RELEASE.md` |
| F-003 | critical | resolved | Localhost server prerequisite was under-specified | `tools/WINDOWS-RELEASE.md:21`; `:54-55`; `tools/check-mdt-release-prereqs.ps1:47` |
| F-004 | critical | resolved | Path fallback bug could select non-existent artifact path | `tools/package-mdt-client-min-online.ps1:67-77`; `tools/verify-mdt-client-min-release-set.ps1:102-113` |
| F-005 | high | resolved | Rust parity gradle task semantics were ambiguous | `build.gradle:451-470`; `tests/src/test/java/ApplicationTests.java:6885-6896` |
| F-006 | high | resolved | `clientSnapshot` plans queue exceeded Java network cap risk | `rust/mdt-client-min/src/client_session.rs:2491-2503`; `:4153-4173`; `:4212-4225` |
| F-007 | high | resolved | `clientSnapshot` mining tile path gap | `rust/mdt-client-min/src/client_session.rs:2442-2445`; `:4059` |
| F-008 | high | resolved | `mdt-world` generator path depended on current working directory | `rust/mdt-world/src/main.rs:13-14`; `:614-624` |
| F-009 | high | resolved | Split Rust workspace checks were not explicit | `tools/verify-rust-workspaces.ps1`; local run `verified_rust_workspaces` |
| F-010 | high | resolved | Release preflight for scripts/fixtures was missing | `tools/check-mdt-release-prereqs.ps1`; local run `release_prereq_result: status=ok` |
| F-016 | high | resolved | Session/network hardening landed: remote ping/pingResponse handling + transport loop fixes | `rust/mdt-client-min/src/client_session.rs:140-146`; `:1723-1732`; `:1856-1873`; `rust/mdt-client-min/src/arcnet_loop.rs:84-125` |
| F-011 | medium | resolved | Release-set verify lacked optional split-workspace switch-through | `tools/package-mdt-client-min-release-set.ps1:23-28`; `:49-51`; `:128-131`; `tools/verify-mdt-client-min-release-set.ps1:19-24`; `:46-49`; `:157-167` |
| F-012 | medium | resolved | Final publish/no-publish checklist is now frozen with explicit owners/commands/markers/failure handling | `Frozen Go/No-Go Checklist (Track P1)` section in this file |
| F-013 | medium | resolved | Preflight enforcement policy landed: release-set verify runs preflight by default with explicit bypass switch | `tools/verify-mdt-client-min-release-set.ps1:26-29`; `:140-147`; `:306`; `tools/package-mdt-client-min-release-set.ps1:30-32`; `:134-136`; `tools/WINDOWS-RELEASE.md:47`; `:57-58`; `:68-69` |
| F-017 | medium | resolved | `mdt-client-min-online` CLI coverage improved (args/features tests expanded for snapshot/build/chat/render/world-stream and outbound action paths) | `rust/mdt-client-min/src/bin/mdt-client-min-online.rs:753-768`; `:802-966`; `:1058`; `:3139-3164`; `:3435-3526`; `:3658-3854` |
| F-014 | medium | resolved | CI release gate workflow ownership/policy closure is frozen, with stable required-check context and governance routing | `.github/workflows/rust-release-gate.yml:1`; `:39-52`; `:118-129`; `audit/ci-gate-plan.md`; `CODEOWNERS`; `tools/README.md` |
| F-015 | medium | resolved | Transitional fixture fallback cleanup reached R+2 canonical-only closure in release-facing scripts | `audit/transitional-fixture-cleanup.md:9`; `:115-133`; `tools/check-mdt-release-prereqs.ps1:21-28`; `:39-47`; `tools/package-mdt-client-min-online.ps1:95-99`; `:115-120`; `tools/verify-mdt-client-min-release-set.ps1:53-60`; `:128-137`; `:306`; `tools/WINDOWS-RELEASE.md:60-69` |
| F-018 | medium | resolved | Minimal remote-control/runtime observability coverage widened: `connect(String,int)`, minimal `kick(serverRestarting)` runtime reconnect scheduling/execution, `playerDisconnect`, `setCameraPosition`, `sound`, `soundAt`, minimal `effect(..., data)` object parsing/storage plus fixed-shape `effect` / `effectReliable`, `traceInfo`, `setRules`, `setObjectives`, `setRule`, `clearObjectives`, `completeObjective`, `constructFinish` full config tracking, inbound `tileConfig` minimal observability, outbound `commandUnits` + broader outbound action queue wiring, and custom packet families now have explicit runtime watch + `--print-client-packets` event output | `rust/mdt-client-min/src/client_session.rs:188-293`; `:813-853`; `:6173-6331`; `rust/mdt-client-min/src/bin/mdt-client-min-online.rs:753-768`; `:802-966`; `:2554-2788`; `:2891-2970`; `tests/src/test/java/ApplicationTests.java:6709-6723`; `tests/src/test/resources/control-packet-goldens.txt` |

## Remaining Problems (Parallelizable)

### Track P1 (`resolved`) Final Release Checklist Closure
- Scope: convert F-012 into a frozen go/no-go block with named owners and execution order.
- Closure evidence: `Frozen Go/No-Go Checklist (Track P1)` section + `Final Decision Block (Frozen)`.

### Track P2 (`resolved`) Preflight Enforcement Decision
- Decision: `mandatory-by-default with explicit opt-out`
- Policy:
  - default release-set verify flow executes `check-mdt-release-prereqs.ps1`
  - exceptional flows can bypass via `-SkipPreflight`
  - release-facing paths are `canonical-only` at R+2 (`fixtures/...`); transitional `rust/fixtures/...` paths are hard-failed
- Evidence:
  - `tools/verify-mdt-client-min-release-set.ps1` now runs preflight unless skipped
  - `tools/package-mdt-client-min-release-set.ps1` forwards preflight control switches to verify
  - `tools/WINDOWS-RELEASE.md` documents default behavior + canonical-only fixture policy

### Track P3 (`resolved`) CI Gate Integration
- Scope: define and land CI job sequence for release readiness.
- Input: `tools/verify-rust-workspaces.ps1`, `tools/check-mdt-release-prereqs.ps1`, `tools/verify-mdt-client-min-release-set.ps1`.
- Output: CI plan/tasks with trigger conditions and required environment (localhost server note if needed).
- Done when: CI ownership + expected artifacts/log markers are documented.
- Landed now: `.github/workflows/rust-release-gate.yml` executes prereqs -> workspaces -> localhost server smoke -> release verify -> artifact upload.
- Closed now: required-check context, owner/backup map, rerun/waiver policy, artifact retention, and governance routing are frozen in `audit/ci-gate-plan.md`, workflow markers, and `CODEOWNERS`.
- Report: [audit/ci-gate-plan.md](audit/ci-gate-plan.md)

### Track P4 (`in-progress`, M6) Protocol/Remote Coverage Audit
- Scope: re-audit remote packet and registry coverage against Java export manifest for non-release-blocking parity backlog.
- Input: `build/mdt-remote/remote-manifest-v1.json`, `rust/mdt-remote`, `rust/mdt-client-min`.
- Manifest path relationship (release policy): `build/mdt-remote/remote-manifest-v1.json` is the generated build artifact source; `fixtures/remote/remote-manifest-v1.json` is the canonical release fixture mirror; release scripts reject transitional `rust/fixtures/...` paths at R+2.
- Output: delta list of packet families (`covered`, `partial`, `missing`).
- Done when: backlog item IDs and severities are assigned for all missing families.
- Dispatch-first worker lanes: `audit/m6-m9-subagent-write-lanes-20260324.md` (`M6-1` .. `M6-3`)
- Recent landed updates: `connect(String,int)` now also has a minimal online-runtime redirect execution path, `kick(serverRestarting)` now also has a minimal online-runtime reconnect execution path against the current server, `playerDisconnect`, `setCameraPosition`, `sound`, `soundAt`, broader `effect(..., data)` object parsing/storage plus event-level `data_object` carriage, lightweight business projections for the highest-signal effect-data tags (now including entity-table `UnitId`, first-element `Point2[]` / `Vec2[]` position discovery, bounded nested `object[]` discovery, structured `object[]` kind summaries, explicit `ParentRef` semantics for `BuildingPos` / `UnitId`, and typed `ContentRef` distinction between raw `Content` vs raw `TechNode`), minimal recent-effect runtime scene markers, and fixed-shape `effect` / `effectReliable`, effect-data parse-fail/trailing observability, `traceInfo`, outbound `adminRequest`, outbound `requestDebugStatus`, inbound `debugStatusClient` / `debugStatusClientUnreliable`, `setRules`, `setObjectives`, `setRule`, `clearObjectives`, `completeObjective`, strict inbound `beginPlace` appended `TypeIO` config tracking, explicit inbound `ping` / `pingResponse` session events, `constructFinish` full appended `TypeIO` config tracking, inbound `tileConfig` minimal event/state tracking with parse-fail observability, event-level authoritative apply/rollback outcome tracking, and configured outcome classification (`Applied` / `RejectedMissingBuilding` / `RejectedMissingBlockMetadata` / `RejectedUnsupportedBlock` / `RejectedUnsupportedConfigType`), and a lightweight authoritative building table keyed by `build_pos` that now updates from `blockSnapshot` first-entry fixed-prefix data plus `constructFinish` / `tileConfig` / `deconstructFinish` and clears on `worldDataBegin`. `RulesProjection` now also preserves `pvp/canGameOver/coreCapture/reactorExplosions/schematicsAllowed/fire/unitAmmo/ghostBlocks/logicUnitControl/logicUnitBuild/logicUnitDeconstruct/blockWhitelist/unitWhitelist/winWave/unitCap/disableUnitCap/defaultTeam/waveTeam/initialWaveSpacing` in addition to the earlier wave/economy subset, while `ObjectivesProjection` now also preserves lightweight `hidden/details/completionLogicCode/team_id/flagsAdded/flagsRemoved` metadata. `stateSnapshot` business projection now also preserves lightweight apply semantics (`Playing/Paused/GameOver` transitions, wave-advance from/to, `timeData` delta/rollback/apply counters, and compact core-inventory total/nonzero/changed-team summaries). That fixed-prefix/base parse now covers `health/rotation/team/ioVersion/enabled/moduleBitmask/timeScale/timeScaleDuration/lastDisabler/legacyConsumeConnected/efficiency/optionalEfficiency/visibleFlags` for the first entry, and the building table now preserves minimal `block_id/rotation/team/io_version/module_bitmask/time_scale_bits/time_scale_duration_bits/last_disabler_pos/legacy_consume_connected/enabled/config/health/efficiency/optional_efficiency/visible_flags/last_update` facts across packets. `constructFinish` now also seeds the tracked authoritative config map for that building, while `deconstructFinish` clears stale config state for removed buildings. Session-level custom text/binary handler registration+dispatch across `clientPacket*` / `clientBinaryPacket*` and inbound `serverPacket*` / `serverBinaryPacket*`, plus inbound `clientLogicData*` event/state observability and session-level channel handler registration+dispatch, are also landed. `ClientSession` now also exposes minimal outbound queue coverage for `clientPacket*` / `clientBinaryPacket*` / `clientLogicData*` across reliable and unreliable paths. The online runtime also now exposes custom-packet observability via `--watch-client-packet` / `--watch-client-binary-packet` / `--watch-client-logic-data`, structured event printing via `--print-client-packets`, scheduled outbound injection via `--action-client-packet` / `--action-client-binary-packet` / `--action-client-logic-data`, and a stable outbound-action script regression test so event signatures can be replay-checked. `mdt-typeio` now also covers a much broader object subset across raw `Content`, `IntSeq`, `Point2[]`, raw `TechNode`, `Team`, `LAccess`, `double`, raw `Building`, legacy unit-command-null marker, `boolean[]`, raw `Unit`, `Vec2[]`, `UnitCommand`, and Java-correct `int[]` short-length semantics, and now additionally exposes a reusable left-to-right DFS helper for nested object discovery (`find_first_dfs`) that returns the matched object plus its path. The local builder/runtime path now also keeps a minimal authoritative queue projection (`Queued` / `InFlight` / `Finished` / `Removed` plus orphan-authoritative counting) driven by `BeginPlace` / `RemoveQueueBlock` / `ConstructFinish` / `DeconstructFinish`, with same-tile replacement now following Java `BuilderComp.addBuild(...)` more closely via `(x,y)` dedupe semantics and a minimal queue-head view; `mdt-input` action-edge ordering is now stable across input permutations, and `rotate_plans` now supports multi-step direction normalization instead of assuming a single-step turn. Inbound command/control families now also have a first pure-observability pass for `buildingControlSelect` / `unitClear` / `unitControl` / `commandBuilding` / `commandUnits` / `setUnitCommand` / `setUnitStance` across session state, events, and online `--print-client-packets` summaries.
- Additional observability closure on top of that runtime slice: `sound` / `soundAt`, `traceInfo`, `debugStatusClient*`, `setRules`, `setObjectives`, `setRule`, `clearObjectives`, and `completeObjective` now also surface through compact runtime HUD labels (`runtime_audio`, `runtime_admin`, `runtime_loading`, `runtime_rules`) or explicit `--print-client-packets` summary lines. Truncated `sound` / `soundAt` / `traceInfo` / `debugStatusClient*` payloads now also increment dedicated parse-fail counters in session state, and the audio/admin HUD labels carry those fail counts directly. `runtime_rules` now also carries a bounded business summary for objective qualification and flag state (`qualified` count, parent-edge count, objective-flag count) instead of only raw packet counters.
- Additive command/control observability completion: inbound package #8 `unitBuildingControlSelect` now has pure observability across `ClientSessionEvent`, `SessionState`, runtime HUD counters, and online `--print-client-packets` summaries. This is the completion of the first command/control observability batch.
- Additional landed remote packet slice: inbound `takeItems`, `transferItemTo`, `transferItemToUnit`, `payloadDropped`, `pickedBuildPayload`, `pickedUnitPayload`, and `unitDespawn` now have explicit decode/event/state paths (instead of ignore-only fallback), while outbound queue/runtime wiring now also includes `setUnitCommand` and `setUnitStance` with Java-shaped payload encoding. `takeItems` / `transferItemTo` / `transferItemToUnit` now additionally feed a unified bounded `resource_delta_projection` (`last kind/item/amount/targets` plus per-family counts) surfaced in runtime HUD `runtime_resource_delta`, rather than living only as three disjoint last-packet observations.
- Additional decode-only remote observability closure: inbound `createBullet`, `destroyPayload`, and `transferItemEffect` now also have explicit manifest-bound decode/event/state paths plus summary output instead of falling through to generic `IgnoredPacket`.
- Additive inbound observability closure: inbound `setPlayerTeamEditor`, `menuChoose`, `textInputResult`, and `requestItem` now have explicit observability decode paths; this decode batch is compatible with server->client `player`-prefixed variants, and observability decode for `buildingControlSelect`, `unitControl`, `requestBuildPayload`, `requestUnitPayload`, `transferInventory`, `rotateBlock`, and `deletePlans` has been regression-revalidated.
- Additive clipboard/URI/text-input observability closure: inbound `copyToClipboard`, `openURI`, and `textInput` (6/7-arg) now have explicit decode/event/state tracking, runtime HUD counters, and online `--print-client-packets` summaries.
- Additive HUD/menu/resource observability closure: HUD/UI notice paths `setHudText`, `setHudTextReliable`, `hideHudText`, `announce`, `infoMessage`, `infoToast`, `warningToast`, `infoPopup` (with/without `id`), `infoPopupReliable` (with/without `id`), `copyToClipboard`, and `openURI`; menu lifecycle paths `menu`, `followUpMenu`, `hideFollowUpMenu`, and `textInput` (6/7-arg); and resource mirror paths `setItem`, `setItems`, `setLiquid`, `setLiquids`, `setTileItems`, `setTileLiquids` now have explicit decode/event/state tracking, runtime HUD counters, and online `--print-client-packets` summaries.
- Additive world-label observability closure: `label` (4/5-arg), `labelReliable` (4/5-arg), and `removeWorldLabel` now have explicit decode/event/state tracking, runtime HUD counters (`runtime_world_label`), and online `--print-client-packets` summaries.
- Additive gameplay-signal observability closure: inbound `setFlag`, `gameOver`, `updateGameOver`, `sectorCapture`, and `researched` now have explicit decode/event/state tracking plus online `--print-client-packets` summaries.
- Additive `mdt-typeio` effect-data summary closure: `TypeIoObject::effect_summary` / `effect_summary_bounded` now provide stable effect kind summaries plus first semantic-parent/position hints (including first-element `Point2[]` / `Vec2[]` paths), and `mdt-client-min` now uses that summary output for effect-data kind labeling.
- Build/codegen minimal viable chain is now connected: Gradle tasks can generate `build/mdt-remote/remote-manifest-v1.json` and refresh `rust/mdt-client-min/src/generated/remote_high_frequency_gen.rs`.
- Scope guardrail: `setRules` / `setObjectives` are landed only as `minimal compatibility client` protocol handling, not full Java `Rules` / `MapObjectives` semantic parity.
- Remaining M6 high-complexity packet slice: Java-equivalent `effect(..., data)` business semantics on top of `TypeIO.readObject` are still not implemented.
- Recommended next engineering focus: M7 deeper snapshot semantics (`entity` / `block` / `hidden` application breadth + `stateSnapshot.coreData` semantic apply), keeping release wording inside `minimal compatibility client`.
- Report: [audit/remote-packet-coverage.md](audit/remote-packet-coverage.md)

### Track P5 (`in-progress`, M7) Session/Network Behavior Audit
- Scope: audit `mdt-client-min` session behavior parity boundaries.
- Input: `rust/mdt-client-min/src/client_session.rs`, online harness behavior logs.
- Output: list of server->client packet behaviors still handled as minimal/no-op.
- Done when: each behavior is tagged `release-critical` vs `parity-backlog`.
- Dispatch-first worker lanes: `audit/m6-m9-subagent-write-lanes-20260324.md` (`M7-1` .. `M7-3`)
- Recent landed updates: remote ping + pingResponse handling and transport loop hardening are merged; inbound `ping` / `pingResponse` now also surface explicit session events; default snapshot cadence now tracks Java's 4-tick baseline more closely; timeout defaults now use a 30min connect floor plus 20s ready-state snapshot stall floor; ready-state snapshot timeout anchor is now armed on connect-confirm and refreshes on `EntitySnapshot` only (no longer refreshed by `StateSnapshot`); `stateSnapshot` now has minimal header tracking plus a lightweight `team -> items` `coreData` projection with parse-failure telemetry in Rust session state, derives a business/runtime projection for `wave/enemies/paused/gameOver/timeData/tps/rand/core inventory`, and now also keeps a separate session-authoritative mirror that always applies header fields, preserves the last-good core inventory across malformed `coreData`, tracks `gameOver > paused > playing` precedence, wave-advance-only-on-increase semantics, core-parse-fail counts, and is cleared on `worldDataBegin`; runtime HUD now prefers that authority mirror for snapshot labels. `entitySnapshot` now records envelope/apply observability, writes the local player into a minimal entity table row (`entity_id -> class/local/unit/position/hidden`), and also batch-upserts any additional parseable `classId=12` player rows into the entity table (without yet claiming full Java entity-system parity); `blockSnapshot` / `hiddenSnapshot` now also have minimal structured envelope parsing with dedicated parse-failure telemetry (`amount`/`data_len`, `count`/`first_id`), and `blockSnapshot` now also parses the first entry's fixed prefix/base (`health/rotation/team/ioVersion/enabled/moduleBitmask/timeScale/timeScaleDuration/lastDisabler/legacyConsumeConnected/efficiency/optionalEfficiency/visibleFlags`) into both a lightweight runtime head projection and the authoritative building table. When a loaded world bundle is available, Rust can now additionally walk and apply later `blockSnapshot` entries beyond the first by using the local loaded-world block/revision context to recover safe sync boundaries, but this still only lands base-field/business-table updates rather than full Java `tile.build.readSync(...)` parity. `hiddenSnapshot` now behaves as a one-shot trigger rather than a persistent hidden-id set, applying hide/remove only to the current payload's IDs while still exposing bounded trigger-count observability and minimal non-local lifecycle removal. The building table keyed by `build_pos` now preserves minimal `block_id/rotation/team/io_version/module_bitmask/time_scale_bits/time_scale_duration_bits/last_disabler_pos/legacy_consume_connected/enabled/config/health/efficiency/optional_efficiency/visible_flags/last_update` facts across `blockSnapshot` / `constructFinish` / `tileConfig` / `deconstructFinish` / `buildHealthUpdate`; runtime HUD/refresh now surfaces snapshot summaries together with the new state/effect business-apply labels, the new building-table summary label, block fixed-prefix diagnostics, explicit runtime config / config-rollback markers for authoritative `tileConfig`, a minimal builder queue head label, broader input-side observability, and recent effect markers in the runtime scene; world-stream begin packets now enforce a 64 MiB hard cap before buffer allocation; Java-style `clientLoaded` gating is now approximated during active world-data load (`StreamBegin/StreamChunk` pass through, normal-priority inbound packets defer and replay once bootstrap/world-ready marks `client_loaded`, low-priority inbound packets are dropped, and `worldDataBegin` clears the deferred queue); and `worldDataBegin` now also clears lightweight rules/objectives projections, the entity table, the builder queue projection, the building table, `tile_config_projection`, and both snapshot business + authority state so a new world does not inherit prior-world business state.
- `hiddenSnapshot` projection now also tracks latest-set semantics (`hidden_snapshot_ids`) and a real delta surface (`added`/`removed` versus previous payload), and runtime HUD now exposes both current and delta labels (`runtime_hidden`, `runtime_hidden_delta`) for this path.
- Additional M7 observability closure: runtime HUD now also exposes compact audio/admin counters (`runtime_audio`, `runtime_admin`) together with loading/snapshot gate counters (`runtime_loading`) so `sound` / `soundAt`, `traceInfo`, `debugStatusClient*`, deferred/replayed inbound packets, dropped loading-time low-priority packets, and state/entity snapshot parse failures are visible without deep logs. Those audio/admin labels now also include per-family parse-fail counts for truncated inbound payloads.
- Additional M7 applied-event closure: parse-success `stateSnapshot` packets now emit a dedicated `StateSnapshotApplied` event plus `--print-client-packets` summary output, and runtime HUD now exposes a compact `runtime_snap_apply=...` label so semantic state changes are no longer only implicit inside aggregate snapshot/status text.
- Additional M6 authority cleanup: inbound `removeTile` now clears authoritative building/config projections like `deconstructFinish`, and loading-time low-priority drops now emit `DroppedLowPriorityPacketWhileLoading` instead of only generic `IgnoredPacket`.
- Additional M9 HUD structure: `mdt-render-ui` `HudSummary` now also exposes `overlay_visible`, `fog_enabled`, `visible_tile_count`, and `hidden_tile_count` for structured visibility/fog presentation without reparsing compact status text.
- Additional M7 hardening landed: `blockSnapshot` first-entry head application now skips conflicting overwrites when an authoritative tracked building row already exists at the same `build_pos` with mismatched `block_id/team/io_version`, keeping the prior row intact while surfacing explicit conflict-skip telemetry; and `entitySnapshot` local-player extraction now requires a unique parseable `player_id + classId(12)` hit instead of accepting the first parseable sliding-window match, with explicit ambiguity counters/last-match observability so duplicate/false-positive hits no longer silently rewrite local player state.
- Additional M7 hardening landed: the loaded-world `blockSnapshot amount > 1` extra-entry path is now fail-closed instead of partially applying a prefix on parse drift, and it additionally requires first-entry consistency with the already parsed head projection before later entries are accepted; `entitySnapshot` now also keeps a short-lived tombstone guard for recently removed entity IDs so `unitDespawn` / `unitEnteredPayload` / `playerDisconnect` / hidden-lifecycle removals are not immediately revived by a stale near-following player-row snapshot. These remain minimal safety rails, not full Java live-world parity.
- Additional M7 hardening landed: `stateSnapshot` now keeps explicit `last_good_state_snapshot_core_data` retention and runtime fallback usage for core-inventory summary surfaces when current `coreData` parsing fails; `entitySnapshot` parseable-player-row application now enforces the declared envelope `amount` threshold (fail-closed on overflow rows); and hidden non-local entities now remain blocked from `entitySnapshot` re-upsert while their IDs stay present in `hidden_snapshot_ids`.
- Additional M7 payload recursion closure landed: loaded-world `entitySnapshot` payload-family rows and `mdt-world` payload-family tails now have boundary-safe recursive `UnitPayload` body consumption for the currently covered vanilla unit revision families (`alpha/beta/gamma/arkyid/toxopid`, `block/flare/risso/corvus`, `mono`, `poly`, `spiroct`, `mech`, explicit payload `mega/oct/quad`, `building-tether`, `timed-kill`, and the current `tank/elevation/crawl` shapes). The payload family is no longer forced through one merged legacy layout: `oct` early `flag/no mineTile` revisions and `quad rev6` abilities are now consumed by their own exact shapes. Unknown or unmapped unit class-id families still fail closed instead of drifting row boundaries.
- Additional M7 regression-fixture closure: Java now emits `tests/src/test/resources/unit-payload-goldens.txt` with real serialized `UnitPayload` bodies for `alpha`, `flare`, `mono`, `poly`, `mace`, `mega`, `quad`, `oct`, `manifold`, `quell-missile`, `spiroct`, `stell`, `vanquish`, `elude`, and `latum`; Rust `mdt-world` consumes that resource directly in parser tests, so payload-family regressions are checked against current Java wire bytes instead of synthetic-only samples.
- Additional M7 audit closure: compatibility aliases `classId=1/25/40/47` are legacy-only IDs preserved in `classids.properties`; current vanilla `EntityMapping` uses `24/4/43/46` for the active `atrax/vela/vanquish/renale` rows. Rust may keep those old IDs only as compatibility parsing paths, not as current-vanilla evidence.
- Remaining release-critical gap: snapshot/model parity still has non-trivial divergence (override-driven behavior vs Java live-input model), especially broader `entity` / `block` / `hidden` application depth and full `coreData` semantic application into live world/game systems.
- Report: [audit/client-snapshot-parity.md](audit/client-snapshot-parity.md)

### Track P6 (`in-progress`, M8) Input/Gameplay Parity Audit
- Scope: audit commands/chat/build/mining/movement paths for parity depth beyond minimal closure.
- Input: `rust/mdt-input`, `rust/mdt-client-min`.
- Output: interaction matrix (`implemented`, `minimal`, `missing`) with test evidence targets.
- Done when: matrix covers all shipped CLI/input capabilities and known Java-side interactions.
- Dispatch-first worker lanes: `audit/m6-m9-subagent-write-lanes-20260324.md` (`M8-1` .. `M8-3`)
- Recent landed updates: `mdt-client-min-online` CLI option/test coverage expanded for build/chat/render/world-stream and snapshot override paths; `mdt-input` `rotatePlans/flipPlans`-style plan editing is now wired into the online CLI path; `mdt-input` also now exposes a minimal stateful action-edge mapper (`ActionPressed` / `ActionHeld` / `ActionReleased`) with duplicate-action dedupe at the library layer, now keeps a stable action-edge emission order across input permutations / multi-release drops, and `rotate_plans` now normalizes multi-step direction turns instead of only handling one-step cases. The online runtime now exposes queued `requestItem`, `requestUnitPayload`, `unitClear`, `unitControl`, `unitBuildingControlSelect`, `buildingControlSelect`, `clearItems`, `clearLiquids`, `transferInventory`, `requestBuildPayload`, `requestDropPayload`, `dropItem`, `rotateBlock`, `tileConfig`, `tileTap`, `deletePlans`, `commandBuilding`, and `commandUnits`, plus broader build-plan config creation across a wider `TypeIO` subset (`Int` / `Long` / `Float` / `Bool` / `IntSeq` / `Point2` / `Point2[]` / `TechNodeRaw` / `Double` / `BuildingPos` / `LAccess` / `String` / `byte[]` / `LegacyUnitCommandNull` / `boolean[]` / `UnitId` / `Vec2` / `Vec2[]` / `Team` / `int[]` / `object[]` / raw `Content` / `UnitCommand`). The `--action-tile-config` CLI subset is also broad, the online runtime now has a stable outbound-action script regression test for event signatures, and the runtime HUD now reports counts for the supported build-plan config families together with a minimal authoritative builder queue label. `ClientSession` now also exposes a minimal builder queue projection driven by `BeginBreak` / `BeginPlace` / `RemoveQueueBlock` / `ConstructFinish` / `DeconstructFinish` so Rust can surface `Queued` / `InFlight` / `Finished` / `Removed` plus orphan-authoritative state while local plans reconcile against server events, with same-tile place/break replacement now following Java `BuilderComp.addBuild(...)` more closely via `(x,y)` dedupe semantics.
- M8 outbound command surface now also includes `setUnitCommand` and `setUnitStance` in both `ClientSession` queue APIs and online CLI action wiring, and runtime HUD now additionally surfaces inbound counters for `takeItems` / `transferItemTo` / `transferItemToUnit` / `payloadDropped` / `pickedBuildPayload` / `pickedUnitPayload` / `unitDespawn`.
- M8 runtime-input closure widened again: `RuntimeIntentTracker` now continuously samples runtime `InputSnapshot` state even without scheduled `--intent-snapshot` injections, while still keeping schedule overrides authoritative until replaced. This narrows one real schedule-only gap without claiming Java desktop/mobile parity.
- Additional M8 runtime-input hardening landed: `snapshot_input.building` inside `mdt-client-min` now follows the authoritative builder queue projection (`BeginPlace` / `BeginBreak` / `RemoveQueueBlock` / `ConstructFinish` / `DeconstructFinish`) instead of staying CLI/override-only, so Rust runtime input clears/sets `isBuilding` in step with the current minimal queue view even when queue state changes arrive from inbound packets.
- Additional M8 mapper closure landed: `mdt-input` now exposes `IntentSamplingMode::LiveSampling` plus `LiveIntentState` semantics for runtime-style sampling (press/release edge emission without repeated held-edge spam while action active state remains stable across snapshots).
- Remaining gap: the main M8 deltas are now Java-level builder/input behavior semantics (live-input parity, `BuilderComp` queue logic, `beginBreak`/full runtime exposure, and richer end-to-end behavior fidelity), not the old narrow config-type coverage baseline.
- Report: [audit/input-build-plan-parity.md](audit/input-build-plan-parity.md)

### Track P7 (`unresolved`, M9) Render/UI Parity Audit
- Scope: audit render/runtime overlay/UI behavior against M9 parity expectations.
- Input: `rust/mdt-render-ui`, runtime overlay behavior in `mdt-client-min`.
- Output: view/runtime parity matrix and remaining deltas.
- Done when: all render/UI deltas are tagged as `release scope excluded` or assigned for follow-up.
- Dispatch-first worker lanes: `audit/m6-m9-subagent-write-lanes-20260324.md` (`M9-1` .. `M9-3`)
- Additive M9 low-risk closure: `mdt-render-ui` now exposes semantic-kind mapping (`player/marker/plan/block/terrain/unknown`) for render object classification, applies stable floor/clamp/zoom normalization in player-centered window crop math, and now also has presenter-local `MinimapPanelModel` / `BuildConfigPanelModel` summaries that surface existing structured HUD/build-inspector data through deterministic `MINIMAP` / `BUILD-CONFIG` panel text. Interactive minimap/UI parity remains backlog/excluded.
- Report: [audit/render-ui-parity.md](audit/render-ui-parity.md)

### 2026-03-23 High-Priority Parity Delta Refresh
- `high` Session/network parity still concentrates around `clientLoaded` defer/replay semantics, deeper `entity/block/hidden/state` snapshot apply, and fuller post-`WorldStream` world application.
- `high` World/content parity still concentrates around post-envelope save-region semantics, entity-region persistence/remap, and Java-equivalent save-region abstractions. Passive `.msav` envelope parsing, `entities` region observability, and post-load class summary helpers are now landed, but business apply remains open.
- `high` Render/input parity still concentrates around real-time keyboard/mouse/touch capture, layered renderer/HUD/build-config UI, and minimap/chat/dialog user flows.
- Working backlog detail is appended in `RUST_RELEASE_AUDIT_CONTINUATION.md` under `2026-03-23 Additional Parity Audit Backlog`.

### Track P8 (`resolved`) Transitional Fixture Fallback Cleanup
- Scope: close transitional fallback removal and enforce canonical-only release fixture policy.
- Input: `tools/check-mdt-release-prereqs.ps1`, packaging/verify scripts, `audit/transitional-fixture-cleanup.md`.
- Output: R+2 hard cutoff closure with canonical-only markers.
- Done now: release-facing scripts removed fallback/waiver switches, hard-fail transitional fixture paths, and emit canonical policy markers (`fixture_policy=canonical_only`).
- Report: [audit/transitional-fixture-cleanup.md](audit/transitional-fixture-cleanup.md)

## Current Release Gate Command Set (Reference)

1. `powershell -ExecutionPolicy Bypass -File .\tools\check-mdt-release-prereqs.ps1`
2. `powershell -ExecutionPolicy Bypass -File .\tools\verify-rust-workspaces.ps1`
3. `powershell -ExecutionPolicy Bypass -File .\tools\package-mdt-client-min-release-set.ps1 -Verify -VerifyRustWorkspaces`

## Frozen Go/No-Go Checklist (Track P1)

Frozen on: `2026-03-21`

| Gate | Owner | Command | Expected Output Marker | Failure Handling |
| --- | --- | --- | --- | --- |
| G1 Preflight (canonical fixtures + scripts) | `ReleaseOperator` | `powershell -ExecutionPolicy Bypass -File .\tools\check-mdt-release-prereqs.ps1` | `release_prereq_result: status=ok issue_count=0` | Mark `NO-GO`; fix missing script/fixture path issues; rerun G1 before any other gate. |
| G2 Split-workspace verification | `ReleaseOperator` | `powershell -ExecutionPolicy Bypass -File .\tools\verify-rust-workspaces.ps1` | `verified_rust_workspaces: metadata_only=False checks=4` | Mark `NO-GO`; fix workspace membership/build/test failure; rerun G2. |
| G3 Release-set verify smoke | `ReleaseOperator` | `powershell -ExecutionPolicy Bypass -File .\tools\package-mdt-client-min-release-set.ps1 -Verify -VerifyRustWorkspaces -Server 127.0.0.1:6567` | `verified_windows_release_set:` and `verify_rust_workspaces=True` and `legacy_stage_state=clean` | Mark `NO-GO`; if smoke fails, verify reachable server and package artifacts, fix root cause, rerun G1->G3 in order. |
| G4 Claim wording gate | `ReleaseManager` | `rg -n "minimal compatibility client|full Java parity is not complete|full parity" README.md tools/WINDOWS-RELEASE.md tools/README.md tools/MINDUSTRY-RUST-HANDOFF.md` | Evidence that release text uses minimal-compatibility wording and contains no full-parity claim | Mark `NO-GO`; correct wording in release-facing docs, then re-run G4 and obtain sign-off. |
| G5 Marker validation sign-off | `QA` | manual review of G1-G4 logs and markers | Sign-off record includes all gate markers and timestamps | Mark `NO-GO`; return failed gate ID to owner, rerun failed gate and re-sign. |

## Messaging Guardrails

- Keep saying: `minimal compatibility client`
- Keep saying: `full Java parity is incomplete`
- Do not claim: `Rust implements all original Mindustry parts`

## Release Wording Boundary

- Can publish now with this wording:
  - `Rust minimal compatibility client release chain is working`
  - `Windows packaging/verify flow is available with workspace + preflight checks`
- Cannot publish with this wording:
  - `Rust client already implements all original Mindustry parts`
  - `Feature parity with Java desktop client is complete`

## Final Decision Block (Frozen)

- Final Decision: `YES (conditional)`
- `YES` conditions:
  - G1, G2, G3, G4, G5 all pass in one release run.
  - G3 smoke server is reachable at `127.0.0.1:6567` (or approved equivalent) during verification.
  - External/internal release claim stays within the allowed wording boundary above.
- Automatic `NO` conditions:
  - Any gate fails or any expected marker is missing.
  - Any release message asserts full Java parity or equivalent claim.
  - QA sign-off (G5) is missing.
